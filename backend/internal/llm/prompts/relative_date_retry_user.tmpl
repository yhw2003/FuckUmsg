请重新核对日期并输出结构化相对日期信息。
只输出 JSON，字段仅允许：base_date、offset_days、week_expr、resolved_date、raw_reasoning_tag。

上下文：
{{.NowContext}}

classicDate: {{.ClassicDate}}
previousLLM: {{.PreviousLLM}}

消息：
{{.Input}}
